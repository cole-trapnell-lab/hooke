% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contrasts.R
\name{decorate_contrast_detectability}
\alias{decorate_contrast_detectability}
\title{Add detectability columns to a contrast table that predates them}
\usage{
decorate_contrast_detectability(
  contrast_tbl,
  alpha = 0.05,
  power = 0.8,
  margin = .DEFAULT_MARGIN_FOLD_CHANGE,
  df = NULL,
  log_scale = c("log", "log10", "log2"),
  overwrite = FALSE
)
}
\arguments{
\item{contrast_tbl}{A contrast table from \code{compare_abundances()}. Must carry
\code{delta_log_abund}, \code{delta_log_abund_se} and \code{delta_p_value}.}

\item{alpha}{Significance level the table was produced at. Must match, or the
intervals and detection limits will not correspond to its q-values.}

\item{power}{Desired power level, used for \code{mdfc80}.}

\item{margin}{Effect size to be powered against, as a fold change. Defaults
to the same value \code{compare_abundances()} uses; see there for why it is
derived from the calling threshold rather than chosen.}

\item{df}{Residual degrees of freedom. Leave \code{NULL} to recover it from the
table, which is what you want unless recovery has refused.}

\item{log_scale}{Log scale the table is on, for exponentiating \code{mdfc80}.
Must match how the table was produced.}

\item{overwrite}{Recompute columns that are already present.}
}
\value{
\code{contrast_tbl} with \code{df_resid}, \code{delta_log_abund_lo},
\code{delta_log_abund_hi}, \code{mdfc80}, \code{power_at_margin}, \code{margin_fold_change} and
\code{contrast_note} added. Existing columns are preserved; \code{mdfc} and \code{power}
are left untouched so nothing downstream breaks mid-migration.
}
\description{
\code{compare_abundances()} gained \code{mdfc80}, \code{power_at_margin} and the rest in
0.0.3. Tables written before that carry \code{power} and \code{mdfc}, which cannot
answer "were we powered to see a phenotype here?" -- \code{power} is Hooke's
\code{observed_power}, a restatement of \code{delta_p_value}, and \code{mdfc} was computed
from a contaminated \code{power} argument. This adds the 0.0.3 columns to such a
table \strong{without refitting the model}.
}
\details{
Nothing needs to be re-estimated because every input is already present or
declared: the detectability columns are functions of \code{delta_log_abund_se},
\code{alpha}, \code{df_resid} and a constant, never of the observed effect.
\code{df_resid} is the only value a legacy table lacks, and it is recovered
exactly by inverting \code{delta_p_value = 2 * pt(-|t|, df)}. All usable rows must
agree on it; if they do not, the table was not produced the way this assumes
and you must pass \code{df} yourself.

Columns already present are left alone unless \code{overwrite = TRUE}, so running
this over a genuine 0.0.3 table is a no-op rather than a fight.
}
